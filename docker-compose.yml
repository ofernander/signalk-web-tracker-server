services:
  signalk-web-tracker-srv:
    build: .
    container_name: signalk-web-tracker-srv
    image: signalk-web-tracker-srv
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
